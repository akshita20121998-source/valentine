<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Will You Be My Valentine?</title>

  <style>
    body {
      margin: 0;
      font-family: "Poppins", sans-serif;
      background: linear-gradient(135deg, #ffdde1, #ee9ca7);
      height: 100vh;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .card {
      background: white;
      padding: 24px;
      border-radius: 22px;
      max-width: 420px;
      text-align: center;
      box-shadow: 0 12px 35px rgba(0,0,0,0.2);
      position: relative;
      z-index: 2;
    }

    h1 {
      color: #ff4d6d;
      margin-bottom: 10px;
    }

    .notice {
      background: #fff0f3;
      border-radius: 12px;
      padding: 12px;
      font-size: 14px;
      margin: 15px 0;
    }

    .date {
      font-size: 20px;
      font-weight: bold;
      color: #ff4d6d;
      margin: 10px 0;
    }

    ul {
      list-style: none;
      padding: 0;
      margin: 15px 0;
      text-align: left;
    }

    li {
      margin: 8px 0;
      font-size: 16px;
    }

    .buttons {
      margin-top: 15px;
      position: relative;
      height: 60px;
    }

    button {
      padding: 12px 18px;
      font-size: 15px;
      border-radius: 30px;
      border: none;
      cursor: pointer;
    }

    #yesBtn {
      background: #ff4d6d;
      color: white;
    }

    #noBtn {
      background: #ccc;
      color: #333;
      position: absolute;
      right: 0;
    }

    .hidden {
      display: none;
      margin-top: 15px;
      font-size: 16px;
      color: #ff4d6d;
      font-weight: bold;
    }

    .ps {
      margin-top: 15px;
      font-size: 13px;
      color: #555;
    }

    .music-btn {
      margin-top: 10px;
      background: transparent;
      color: #ff4d6d;
      border: 1px solid #ff4d6d;
      font-size: 13px;
    }

    /* Floating hearts */
    .heart {
      position: absolute;
      bottom: -20px;
      font-size: 20px;
      animation: floatUp 6s linear infinite;
      opacity: 0.7;
    }

    @keyframes floatUp {
      0% {
        transform: translateY(0) scale(1);
      }
      100% {
        transform: translateY(-110vh) scale(1.5);
        opacity: 0;
      }
    }
  </style>
</head>

<body>

  <!-- Background Music -->
  <audio id="bgMusic" loop>
    <source src="fever-dream-alex-warren.mp3" type="audio/mpeg">
  </audio>

  <div class="card">
    <h1>Will you be my Valentine? 💘</h1>

    <div class="notice">
      ⚠️ <b>Important Valentine Update</b> ⚠️<br>
      I’ll be physically unavailable on Feb 14<br>
      (emotionally available 24x7 🥹)
    </div>

    <div class="date">✨ Rescheduled to FEB 16 ✨</div>

    <p>Same girl. Same feelings.<br>More food. Better planning.</p>

    <ul>
      <li>☑️ Momos</li>
      <li>☑️ Pani Puri</li>
      <li>☑️ BOTH (because this is not a democracy)</li>
    </ul>

    <p><b>So sir…</b><br>Will you be my Valentine on the 16th?</p>

    <div class="buttons">
      <button id="yesBtn" onclick="sayYes()">YES (obviously)</button>
      <button id="noBtn" onmouseover="runAway()">No</button>
    </div>

    <div id="response" class="hidden">
      Correct choice 😌❤️<br>
      Get ready for momos, pani puri & me ✨
    </div>

    <button class="music-btn" onclick="playMusic()">▶ Play Fever Dream</button>

    <div class="ps">
      PS: Saying yes is mandatory.<br>
      PPS: I’ll still steal your pani puri.
    </div>
  </div>

  <script>
    function sayYes() {
      document.getElementById("response").style.display = "block";
      playMusic();
    }

    function runAway() {
      const btn = document.getElementById("noBtn");
      btn.style.transform =
        `translate(${Math.random()*200 - 100}px, ${Math.random()*50}px)`;
    }

    function playMusic() {
      document.getElementById("bgMusic").play();
    }

    // Floating hearts
    setInterval(() => {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.innerText = "❤️";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = 4 + Math.random() * 3 + "s";
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 7000);
    }, 500);
  </script>

</body>
</html>